# Phase 3: Inference API
# Run: uvicorn backend.app.main:app --reload --port 8001

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0

# Optional: OpenAI client for streaming (or use httpx only)
openai>=1.0.0
